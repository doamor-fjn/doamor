import { NgModule } from '@angular/core';
import { IonicPageModule } from 'ionic-angular';
import { RegisterInstPage } from './register-inst';

@NgModule({
  declarations: [
    RegisterInstPage,
  ],
  imports: [
    IonicPageModule.forChild(RegisterInstPage),
  ],
})
export class RegisterInstPageModule {
  
}
